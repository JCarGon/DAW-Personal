{
  "name": "11-unit-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test-ava": "c8 --reporter html ava",
    "tes:watch-ava": "ava --watch --verbose",
    "test:report-ava": "(c8 --reporter lcov ava || echo 1) && node test/sonar.js",
    "test-jest": "cross-env NODE_OPTIONS=--experimental-vm-modules npx jest",
    "test:watch-jest": "cross-env NODE_OPTIONS=--experimental-vm-modules npx jest --coverage",
    "test:report-jest": "(cross-env NODE_OPTIONS=--experimental-vm-modules npx jest --coverage || echo 1) && node test/sonar.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ava": "^5.3.1",
    "c8": "^8.0.1",
    "jest": "^29.7.0",
    "sonarqube-scanner": "^3.1.0"
  },
  "ava": {
    "files": [
      "test/**/*.test.js"
    ]
  }
}
